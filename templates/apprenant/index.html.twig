{% extends 'base.html.twig' %}

{% block title %}
	{{ user.prenom }}
	dashboard
{% endblock %}

{% block stylesheets %}

	<link rel="stylesheet" href="{{ asset('styles/apprenant.css') }}" type="text/css">
{% endblock %}
{% block body %}

	<h1>Bienvenue
		{{ user.prenom }}</h1>

	<h1>{{user.formation.titre}}</h1>
	{# <h1>{{foundUser.nom}}</h1> #}
	{% if matieres %}
		{# <h2>Matières</h2>
		        <table border="1" class="table">
		            <tr>
		                <th>Nom</th>
		                <th>Formateur</th>
		            </tr>
		            {% for matiere in matieres %}
		                <tr>
		                    <td>{{matiere.nom}}</td>
		                     <td>{{ matiere.formateur is null ? 'No formateur assigned' : matiere.formateur }}</td>
		                </tr>
		            {% endfor %}
		        </table> #}
		<section class="main">
			<div class="title">
				<h1>Bienvenue apprenant</h1>
				<p>Nom de la formation</p>
			</div>
			<div class="content">
				<svg width="350" height="350">
					<image xlink:href="./images/apprenant.svg" width="350" height="350"/>
				</svg>
				<div class="board">
					<table class="table">
						<tr>
							<th>Matiere</th>
							<th>Formation</th>
							<th>Formateur</th>
						</tr>
						{% for matiere in matieres %}
							<tr>
								<td td class="column1">{{matiere.nom}}</td>
								<td td>{{matiere.formation}}</td>
								<td>{{ matiere.formateur is null ? 'No formateur assigned' : matiere.formateur }}</td>
							</tr>
						{% endfor %}

					</table>
				</div>
			</div>
		</section>
	{% endif %}

{% endblock %}
